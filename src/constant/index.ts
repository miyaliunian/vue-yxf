const APPID = 'creditP_pay_inset'
const timestamp = new Date().getTime()
const version = '1.0'

const PageSize = 10

const IParaWra = { appId: APPID, timestamp: timestamp, version: version }

export { APPID, timestamp, version, IParaWra, PageSize }
